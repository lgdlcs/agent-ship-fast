# Optional — only needed if you don't use `shipfast auth:login` (browser pairing).
# Stripe TEST secret key (sk_test_...) or claimable-sandbox key (rkcs_test_...):
#   https://dashboard.stripe.com/test/apikeys
#   or: stripe sandbox create --email you@example.com
STRIPE_API_KEY=sk_test_...
